# ============================================================
#  NORA Admin — Backend Flask | Variables d'Environnement
# ============================================================
# ⚠️ Jamais de vraies valeurs ici — ce fichier est versionné.

DB_HOST=localhost
DB_PORT=5432
DB_NAME=nora_v2_db
DB_USER=nora_user
DB_PASSWORD=

FLASK_ENV=development
SECRET_KEY_ADMIN=

# Compte admin initial (lu par scripts/create_admin.py)
ADMIN_EMAIL=
ADMIN_PASSWORD=

# Régénération embeddings (partagé avec le backend public)
AI_API_KEY=
EMBEDDING_MODEL=models/text-embedding-004

# CORS STRICT : uniquement l'interface admin
CORS_ORIGINS_ADMIN=http://localhost:5174
